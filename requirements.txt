fastapi
uvicorn[standard]
httpx
pydantic
python-dotenv

openai          # OpenAI-compatible client (also works with Ollama)

faster-whisper  # STT (Whisper base.en)
TTS             # Coqui TTS
soundfile
numpy